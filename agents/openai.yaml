interface:
  display_name: "Litminer"
  short_description: "API-first literature discovery, verification, and publisher evidence queueing"
  default_prompt: "Use $litminer to discover literature through APIs, verify metadata, triage candidates, and build publisher-page evidence queues."
